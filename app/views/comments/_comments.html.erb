<div id="comments">
  <% @comments.each do |comment| %>
    <div class="comment">
      <p><b><%= comment.created_at.strftime('%D %I:%M%P') %></b>: <%= comment.content %></p>
    </div>
  <% end %>
</div>